{
  "name": "react-mixpanel-provider",
  "version": "1.0.0",
  "description": "mixpanel provider",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "build": "tsc",
    "dev": "tsc --watch",
    "prettier": "npx prettier --write src/* test/* example/*",
    "tslint": "tslint -p tslint -p tsconfig.json",
    "codestyle": "npm run prettier && npm run tslint"
  },
  "husky": {
    "hooks": {
      "pre-commit": "npm run codestyle"
    }
  },
  "author": "spiehdid",
  "license": "MIT",
  "devDependencies": {
    "husky": "^4.2.5",
    "prettier": "^2.0.5",
    "tslint": "^6.1.3",
    "tslint-config-prettier": "^1.18.0",
    "typescript": "^3.9.7"
  },
  "dependencies": {
    "mixpanel-browser": "^2.38.0",
    "react": "^16.13.1"
  }
}
